export enum AlignEnum {
    right = 'right',
    left = 'left',
    center = 'center',
}

export enum VerticalAlignEnum {
    top = 'top',
    bottom = 'bottom',
    middle = 'middle',
}

export enum FlexboxDirectionEnum {
    row = 'row',
    column = 'column',
}

export enum FlexboxAlignEnum {
    center = 'center',
    start = 'flex-start',
    end = 'flex-end',
    justify = 'space-between',
}

export enum VerticalDirectionEnum {
    top = 'top',
    bottom = 'bottom',
}

export enum HorizontalDirectionEnum {
    left = 'left',
    right = 'right',
    center = 'center',
}

export enum LangEnum {
    en = 'en',
    vi = 'vi',
}

export enum DialogTypeEnum {
    error = 'error',
    success = 'success',
    confirm = 'confirm',
    info = 'info',
}

export enum SortTypeEnum {
    ascending = 'asc',
    descending = 'desc',
}

export enum KeyboardEnum {
    enter = 'Enter',
    escape = 'Escape',
    backspace = 'Backspace',
    space = 'Space',
    arrowDown = 'ArrowDown',
    arrowUp = 'ArrowUp',
    arrowLeft = 'ArrowLeft',
    arrowRight = 'ArrowRight',
    delete = 'Delete',
}

export enum ValidationTypeEnum {
    required = 'required',
    email = 'email',
    phone = 'phone',
    existed = 'existed',
    future = 'future',
    custom = 'custom',
}
